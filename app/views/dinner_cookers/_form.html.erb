<%= form_for(@dinner_cooker) do |f| %>
  <% if @dinner_cooker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dinner_cooker.errors.count, "error") %> prohibited this dinner_cooker from being saved:</h2>

      <ul>
      <% @dinner_cooker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>

    <%= f.label :move_in_date %><br>
    <%= f.date_select :move_in_date %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
